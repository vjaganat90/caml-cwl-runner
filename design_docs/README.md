# design_docs

[runner.md](runner.md) is the architecture of the engine: how to read the
tree, the module map, CommandLineTool execute (including confinement),
Glob, and what is not built yet. Signatures live in `lib/*.mli`.
